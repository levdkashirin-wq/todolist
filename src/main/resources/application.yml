spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/todo_db
    username: todo_user
    password: todo_pass
    driver-class-name: org.postgresql.Driver

  sql:
    init:
      mode: never

server:
  port: 8080